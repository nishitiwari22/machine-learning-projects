Problem:
Detect unusual or suspicious transactions.

Dataset:
Credit card transactions dataset (you already have one).

What you’ll do:

Standardize data

Tune eps and min_samples

Identify noise points (outliers)

Interview explanation:
“I used DBSCAN because it can detect noise and irregular clusters, which is ideal for anomaly detection.”
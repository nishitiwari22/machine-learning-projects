Problem:
Cluster customers where boundaries are not clear.

Dataset:

Same dataset as K-Means (to compare results)

What you’ll do:

Apply Gaussian Mixture Model

Show probability of cluster membership

Compare with K-Means results

Interview explanation:
“Unlike K-Means, GMM gave soft clustering, where each customer had a probability of belonging to multiple segments.”